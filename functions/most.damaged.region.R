# Funktion för att dela in pat efter högst skadad region, dvs den med högst ais_last


#most.damaged.region <- function(cleaned.data) {
  #pmax(cleaned.data[,58:107])
  #cleaned.data$most.damaged.region <- pmax(cleaned.data[,58:107])
#}
  
 









 #cleaned.data$max.ais <- max(as.numeric(substr(as.character(cleaned.data[1,7:56], 8, 8)))) , returned.data <- pmax(cleaned.data$(clean_data[,58:107]))
#return(returned.data)