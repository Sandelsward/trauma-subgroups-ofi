#Funkton för att avgöra om det finns en eller fler skador. Dvs fler ais_first.

#number.of.dammages <- function(cleaned.data) {
 # test.vec <- c()
 #max(number.of.dammages, na.rm = FALSE)
  
  #cleaned.data$max.ais <- max(as.numeric(substr(as.character(cleaned.data[1,7:56], 8, 8))))